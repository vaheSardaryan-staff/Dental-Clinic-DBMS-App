<div>
  <a href="/appointments" class="text-sm text-indigo-600 hover:underline">&larr; Back to Appointments</a>
  <h1 class="text-2xl font-bold text-gray-900 mt-2 mb-2">Book Appointment</h1>
  <p class="text-sm text-gray-500 mb-6">Uses <span class="sql-badge badge-procedure">STORED PROCEDURE</span> sp_BookAppointment</p>

  <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 max-w-2xl">
    <form action="/appointments" method="POST">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Dentist *</label>
          <select name="dentist_id" required class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
            <option value="">Select dentist...</option>
            <% dentists.forEach(function(d) { %>
              <option value="<%= d.id %>"><%= d.full_name %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Patient *</label>
          <select name="patient_id" required class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
            <option value="">Select patient...</option>
            <% patients.forEach(function(p) { %>
              <option value="<%= p.id %>"><%= p.first_name %> <%= p.last_name %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Service</label>
          <select name="service_id" class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
            <option value="">Select service...</option>
            <% services.forEach(function(s) { %>
              <option value="<%= s.id %>"><%= s.name %> (<%= s.duration_minutes %>min)</option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Title</label>
          <input type="text" name="title" class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" placeholder="e.g., Regular Checkup">
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Start Time *</label>
          <input type="datetime-local" name="start_time" required class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">End Time *</label>
          <input type="datetime-local" name="end_time" required class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-medium text-gray-700 mb-1">Notes</label>
          <textarea name="notes" rows="2" class="w-full px-3 py-2 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"></textarea>
        </div>
      </div>

      <!-- Trigger Demo -->
      <div class="mt-6 p-4 bg-amber-50 border border-amber-200 rounded-xl">
        <label class="flex items-center gap-2 cursor-pointer">
          <input type="checkbox" name="test_past" class="rounded border-gray-300">
          <span class="text-sm font-medium text-amber-700">Demo: Test trg_PreventPastAppointment</span>
        </label>
        <p class="text-xs text-amber-600 mt-1 ml-6">Check this to attempt booking with a past date. The INSTEAD OF INSERT trigger will block it and show the error.</p>
      </div>

      <div class="mt-6 flex gap-3">
        <button type="submit" class="px-6 py-2 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700 transition text-sm font-medium">Book Appointment</button>
        <a href="/appointments" class="px-6 py-2 bg-gray-100 text-gray-600 rounded-xl hover:bg-gray-200 transition text-sm">Cancel</a>
      </div>
    </form>
  </div>
</div>
