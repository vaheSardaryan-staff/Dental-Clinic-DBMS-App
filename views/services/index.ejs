<div>
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">Dental Services</h1>
      <p class="text-sm text-gray-500 mt-1">Browse and manage available services</p>
    </div>
    <a href="/services/new" class="px-4 py-2 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700 transition text-sm font-medium">+ Add Service</a>
  </div>

  <!-- Category Filter -->
  <div class="flex gap-2 mb-4 flex-wrap">
    <a href="/services" class="px-3 py-1.5 rounded-xl text-sm font-medium <%= !selectedCategory ? 'bg-indigo-600 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200' %>">All</a>
    <% categories.forEach(function(c) { %>
      <a href="/services?category=<%= c.category %>" class="px-3 py-1.5 rounded-xl text-sm font-medium capitalize <%= selectedCategory === c.category ? 'bg-indigo-600 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200' %>"><%= c.category %></a>
    <% }) %>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% services.forEach(function(s) { %>
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-5">
        <div class="flex items-start justify-between mb-3">
          <div>
            <h3 class="font-semibold text-gray-800"><%= s.name %></h3>
            <span class="px-2 py-0.5 rounded text-xs font-medium bg-indigo-50 text-indigo-600 capitalize"><%= s.category %></span>
          </div>
          <span class="text-lg font-bold text-emerald-600">$<%= Number(s.default_cost).toFixed(2) %></span>
        </div>
        <% if (s.duration_minutes) { %><p class="text-sm text-gray-500 mb-2"><%= s.duration_minutes %> min</p><% } %>
        <% if (s.description) { %><p class="text-sm text-gray-600 mb-3"><%= s.description %></p><% } %>
        <div class="flex gap-2 pt-3 border-t border-gray-100">
          <a href="/services/<%= s.id %>/edit" class="px-3 py-1 text-xs bg-gray-50 text-gray-600 rounded hover:bg-gray-100">Edit</a>
          <form action="/services/<%= s.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Delete?')">
            <button class="px-3 py-1 text-xs bg-rose-50 text-rose-600 rounded hover:bg-rose-100">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</div>
