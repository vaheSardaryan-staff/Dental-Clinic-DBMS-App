<nav class="fixed top-0 left-0 right-0 z-50 bg-white border-b border-gray-200 shadow-sm">
  <div class="flex items-center justify-between px-6 h-16">
    <div class="flex items-center gap-3">
      <a href="/" class="flex items-center gap-2">
        <div class="w-9 h-9 bg-blue-600 rounded-lg flex items-center justify-center">
          <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/></svg>
        </div>
        <span class="text-xl font-bold text-gray-800">DoQ <span class="text-blue-600">Dental</span></span>
      </a>
    </div>
    <div class="flex items-center gap-4">
      <div class="flex items-center gap-2 bg-gray-50 rounded-lg px-3 py-2">
        <span class="text-sm text-gray-500">Role:</span>
        <select id="roleSelector" onchange="window.location.href='?role='+this.value" class="text-sm font-medium bg-transparent border-none focus:ring-0 cursor-pointer text-gray-700">
          <option value="admin" <%= currentRole === 'admin' ? 'selected' : '' %>>Admin</option>
          <option value="dentist" <%= currentRole === 'dentist' ? 'selected' : '' %>>Dentist</option>
          <option value="receptionist" <%= currentRole === 'receptionist' ? 'selected' : '' %>>Receptionist</option>
        </select>
      </div>
      <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
        <span class="text-blue-600 font-semibold text-sm"><%= currentRole.charAt(0).toUpperCase() %></span>
      </div>
    </div>
  </div>
</nav>
