<div>
  <h1 class="text-2xl font-bold text-gray-800 mb-2">Indexes Showcase</h1>
  <p class="text-sm text-gray-500 mb-6">11 custom indexes for query performance optimization</p>

  <!-- Index List -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">
    <div class="px-6 py-4 border-b border-gray-200">
      <h3 class="text-lg font-semibold text-gray-800">All Custom Indexes</h3>
      <p class="text-sm text-gray-500 mt-1">Queried from <code class="bg-gray-100 px-1 rounded">sys.indexes</code> + <code class="bg-gray-100 px-1 rounded">sys.index_columns</code></p>
    </div>
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Index Name</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Table</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Column</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% indexes.forEach(function(idx) { %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 font-mono text-sm text-blue-600"><%= idx.index_name %></td>
              <td class="px-6 py-4"><%= idx.table_name %></td>
              <td class="px-6 py-4 font-mono"><%= idx.column_name %></td>
              <td class="px-6 py-4"><span class="px-2 py-1 bg-blue-50 text-blue-600 rounded text-xs"><%= idx.type_desc %></span></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Index Usage Stats -->
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">
    <div class="px-6 py-4 border-b border-gray-200">
      <h3 class="text-lg font-semibold text-gray-800">Index Usage Statistics</h3>
      <p class="text-sm text-gray-500 mt-1">From <code class="bg-gray-100 px-1 rounded">sys.dm_db_index_usage_stats</code></p>
    </div>
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Table</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Index</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Seeks</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Scans</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Lookups</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Updates</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% usage.forEach(function(u) { %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4"><%= u.table_name %></td>
              <td class="px-6 py-4 font-mono text-sm text-blue-600"><%= u.index_name %></td>
              <td class="px-6 py-4"><%= u.user_seeks %></td>
              <td class="px-6 py-4"><%= u.user_scans %></td>
              <td class="px-6 py-4"><%= u.user_lookups %></td>
              <td class="px-6 py-4"><%= u.user_updates %></td>
            </tr>
          <% }) %>
          <% if (usage.length === 0) { %>
            <tr><td colspan="6" class="px-6 py-8 text-center text-gray-400">No usage stats available yet - browse the app to generate index usage</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Explanation -->
  <div class="bg-blue-50 rounded-xl border border-blue-200 p-6">
    <h3 class="text-lg font-semibold text-blue-800 mb-3">How Indexes Help</h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm text-blue-700">
      <div>
        <strong>IX_patients_dentist</strong>
        <p class="text-blue-600 mt-1">Speeds up: "Show all patients for Dr. Smith" - filters by dentist_id</p>
      </div>
      <div>
        <strong>IX_appointments_patient / IX_appointments_dentist</strong>
        <p class="text-blue-600 mt-1">Speeds up: appointment lookups per patient or per dentist</p>
      </div>
      <div>
        <strong>IX_appointments_start</strong>
        <p class="text-blue-600 mt-1">Speeds up: "Show today's appointments" - date range queries</p>
      </div>
      <div>
        <strong>IX_invoices_status</strong>
        <p class="text-blue-600 mt-1">Speeds up: "Show all overdue invoices" - status filtering</p>
      </div>
      <div>
        <strong>IX_payments_invoice</strong>
        <p class="text-blue-600 mt-1">Speeds up: "Total payments for invoice #123" - payment aggregation</p>
      </div>
      <div>
        <strong>IX_treatments_visit / IX_treatment_teeth_treat</strong>
        <p class="text-blue-600 mt-1">Speeds up: treatment and teeth lookups per visit</p>
      </div>
    </div>
  </div>
</div>
